<div class="welcome-container">
    <div class="welcome-card glass-effect">
        <div class="brand">
            <div class="logo">S</div>
            <h1>Sprite Application</h1>
            <p>{{message}} to progress to the dashboard.</p>
        </div>
        <div class="actions">
            <a href="/dashboard/login" class="login-button">Go to Dashboard Login</a>
        </div>
    </div>
</div>

<style>
    .welcome-container {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background: radial-gradient(circle at top right, #eef2ff 0%, #f8fafc 50%, #f1f5f9 100%);
        padding: 20px;
    }

    .welcome-card {
        width: 100%;
        max-width: 440px;
        padding: 48px;
        border-radius: 24px;
        box-shadow: var(--shadow-lg);
        background: rgba(255, 255, 255, 0.9);
        border: 1px solid rgba(255, 255, 255, 0.5);
        text-align: center;
    }

    .brand {
        margin-bottom: 32px;
    }

    .logo {
        width: 48px;
        height: 48px;
        background: linear-gradient(135deg, var(--primary) 0%, #818cf8 100%);
        color: white;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        font-weight: 700;
        margin: 0 auto 16px;
        box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
    }

    .brand h1 {
        font-size: 28px;
        font-weight: 700;
        color: var(--text-main);
        letter-spacing: -0.025em;
        margin-bottom: 8px;
    }

    .brand p {
        color: var(--text-muted);
        font-size: 15px;
    }

    .login-button {
        display: inline-block;
        width: 100%;
        padding: 14px;
        background: var(--primary);
        color: white;
        border: none;
        border-radius: 12px;
        font-size: 16px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.2s ease;
        box-shadow: 0 4px 12px rgba(99, 102, 241, 0.2);
    }

    .login-button:hover {
        background: var(--primary-hover);
        transform: translateY(-1px);
        box-shadow: 0 6px 16px rgba(99, 102, 241, 0.3);
    }
</style>